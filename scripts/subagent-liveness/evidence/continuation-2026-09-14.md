# Continuation acceptance evidence — 2026-09-14

No deployment. The installed fork remains 21.7.0-orb.1, source HEAD `6ad3a28132124e36c228887aaf549fd1c69e76e1`, artifact SHA-256 `0dbdd1483b853c35915483709fbd2fa9956d42b4939778ba129ddd1a18b75ee4`; Pi remains 0.85.1. The changes below are host adapter/tests, not additional fork seams.

## Reproductions and repairs

1. **History publication failure escaped the adapter.** Injecting an SDK `getEntries()` failure during final child release threw instead of returning a typed publication error. After catching it, a duplicate terminal could still finish successfully once reads recovered. `flushPersisted()` now maps source exceptions to `history_source_error`, and aggregate finalization requires ready health. Failure stays visible and activity conservative. `activity-before.log` / `activity-after.log` retain the unit regression, not a DST trace.
2. **Archive watchdog excluded the mandatory quarantine.** The extended browser scenario reached archive correctly but its 60-second watchdog was shorter than the existing 65-second deletion quarantine. Its budget now explicitly includes `DEFAULT_LIFECYCLE_CONSTANTS.deletionQuarantineMs`; named FIFO gates still establish all execution ordering. The corrected scenario passed (~220 s). This is a lifecycle-budget assumption correction, not a timeout increase concealing stalled progress.
3. **Shutdown returned before child tool cleanup.** The installed-SDK trace in `shutdown-before.log` shows `tool:one:abort-observed`, then `shutdown:returned`, while the cleanup gate is closed. Upstream disposal awaits extension hooks, not the execution promise. The host now registers its cancellation/drain handler before upstream teardown, retaining public terminal observers until its owned runs drain. `shutdown-after.log` shows tool exit and durable terminal before teardown returns. One explicit event-loop boundary drains the scripted hooks' microtasks; there is no timed grace period or private manager-promise read.
4. **Start edges were missing.** The bridge treated `started` as redundant admission and discarded it. The `start-edge-before.log` regression requires the durable start record; the bridge now records it for an already-owned run. Installed-SDK scenarios enforce this edge.
5. **Luna omitted delegated outcomes.** Generic root events were filtered out of summary input. The real-SDK regression in `summary-before-corrected.log` demonstrates the missing outcome despite correct aggregate scheduling. The Pi adapter now creates a summary-only status/description projection, without raw child/tool output. The same contract requires one summary after aggregate drain containing the child outcome and root follow-up. `summary-before.log` preserves an earlier fixture typo (undefined operation-ID variable), corrected before the meaningful reproduction; it is not product-failure evidence.
6. **Docker upload assertion sampled the two-frame handoff.** The first full Docker/PostgreSQL run passed 84 tests and failed the existing upload visibility assertion: both the live and persisted `UPLOAD_VERIFIED` paragraphs were present between history publication and `output_retired`. The captured mock trace had exactly one matching model request (rule 8), not duplicated inference. The test now waits for one replicated reply and idle activity, then browser abort-control retirement, before retaining the original strict visibility assertion. It explicitly fails if two persisted replies exist; no `.first()` or weakened visibility assertion is used. Only the diagnostic excerpt is exported: raw mock headers stay in the ignored local log.

## Added coverage

- A shared health/snapshot/request-gate/live-operation oracle runs against both simulated SDK events and real installed-package sessions.
- 200 runtime DST schedules race two child terminals, successor admission, inbox delivery and whole-operation abort, with a retained cleanup owner and assertions at relevant checkpoints. They include shell refusal, dedup/retry after abort and stale callback/wake fencing.
- Composed control-plane DST adds 50 schedules with control-plane restart, an eight-hour wall-clock jump (monotonic time unchanged), delayed snapshots sampled at request acceptance, pull failure and post-commit failure. It asserts no premature idle-stop and complete replication after drain.
- Eleven installed-SDK schedules add independent-child broker refresh and `auth_required` failure, no child model/tool execution after failed credentials, no credential material in root history, root-inline hook exclusion, discovered tools and awaited shutdown.
- Recovery unit contracts cover compaction-independent admission scanning, failure notices not acknowledging interruption, stale terminal operation IDs and persisted completed/error outcomes.
- Browser/process E2E adds relative editing in the ordinary shared checkout, runtime-PID death during an SDK-owned FIFO gate, interruption reported once across two restarts, no child replay/private transcript replication, and stopped-after-recovery archive deleting the workspace while retaining history.

## Validation status

- `npm ci` completed before checks.
- Repository suite: 214 files / 1,648 tests passed; three files / five opt-in tests skipped; infrastructure checks passed.
- Extended installed-SDK, activity and recovery contracts: 3 files / 14 tests passed, including all eleven real-package schedules after the summary/shared-oracle changes.
- Docker 29.1.3 was installed but inactive. `sudo systemctl start docker` brought up the existing configured service; an absent socket was not an environmental blocker. The first full Docker/PostgreSQL result was 84 passed / 1 failed as diagnosed above; all 55 PostgreSQL contracts and the child crash/recovery E2E passed.
- The targeted Docker full-slice validation of the completion-boundary fix is in progress. Final results are recorded here when available.

Raw local command logs are under `.context/subagents-continuation/` (ignored). Selected sanitized reproductions are copied beside this index. Remaining acceptance obligations are only in `TODO.md`: in particular, ordinary discovered tools/root-hook exclusion do not establish approved child MCP access, and archive after recovery does not establish active-child destructive races. Native/cloud and everyday-use qualification are not claimed.
