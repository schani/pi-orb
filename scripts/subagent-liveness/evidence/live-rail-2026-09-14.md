# N1 receipts + C2 live rail / P1 terminal coexistence

Implemented 2026-09-14; validation evidence through 2026-09-15. The mobile terminal button remains in overflow. No production deployment or fork change.

## Evidence download

[Checksum-indexed evidence archive](https://files---pi-orb-1077475695242.us-central1.run.app/s/58efed98-b832-4025-a899-7f43fed7ed72/qualification/pr-44/live-rail-evidence.tar.gz)

- 23,238 bytes; SHA-256 `7fd9946d4d77d72a70206a4dee113414b61722aa865c615049116bb68b6eacfd`.
- Manifest includes original paths, sizes/hashes and implementation/test source hashes against parent `e64c46d`. All archive members were byte-verified after packaging.
- Synthetic test output and the original TDD trace only. Live conversation, credentials, browser screenshots/cores and private service state are excluded.

## Tests first and failure explanations

- First regressions: **6 failed / 42 passed**. Structured subagent receipts were absent, the reducer had no child roster and the runtime exposed no roster. Original trace `test-failures/subagent-live-roster-1789428131015-0.json` reproduced the missing-roster assertion before implementation. It contains **zero entropy records** because the assertion precedes scheduling. Replaying after repair passes that boundary, then explicitly exhausts the empty trace at `run-start`; it cannot certify the newly reachable scheduling suffix. The repaired scenario runs 30 fresh schedules, fencing old callbacks against a resumed execution. Diagnostic output now exposes the simulation error rather than only `false`.
- **54 focused tests passed**, including synchronization. After the diagnostic improvement, all **9 runtime activity/DST tests passed** again.
- First browser fixture incorrectly patched a non-running orb's HTTP view while its underlying fixture WebSocket stayed unavailable. The corrected scenario uses the running fixture and owns its live frames/history.
- A terminal assertion incorrectly required one connection from initial mount. Captured edges proved both opens happened during initial StrictMode mounting, before rail/reconnect changes. The regression now establishes readiness and requires no replacement thereafter, matching the existing terminal test's contract.
- The existing terminal geometry regression caught a genuine extra border pixel: 24 versus expected 23. Portaling into a borderless header stack needs `top: calc(100% - 1px)` to overlap its final rule. The old textual CSS contract was updated to that selected anchor; the browser's exact single-rule assertion remains unchanged.
- An initial whole-repository driver was interrupted by this tool's 120-second command deadline, not a failing test. It was confirmed gone before a separate full run; no missing exit was called a pass.

## Results and limits

- **220 files / 1,707 repository tests passed**, 3 files / 5 intentional skips; infra checks passed, including 26 native guest tests.
- Typecheck and lint passed (two pre-existing lint notices remain outside this slice).
- **Full E2E is incomplete:** 9 files / **106 tests passed**, while 4 full-slice scenarios never ran because their Docker image export failed on a missing parent snapshot. Later fixture builds do not clear it. Original output and exit 1 are in the archive; forensics and release rule: `docs/postmortems/2026-09-15-docker-snapshot-validation-failure.md`.
- Passing E2E coverage includes all **45 frontend cases** (31 Chromium session tests plus 14 Chromium/WebKit phone cases), real child-only roster before/after reload, completion/abort/crash drain, active-child archival and authenticated MCP. The three new 1280/390/320px cases cover readable error receipts, separate phase counts, rail/terminal/composer geometry, phone overflow placement, shortened viewport, agent reconnect, final child removal, retained draft and unchanged emulator/socket after readiness.
- Original native WebKit compositor and native/cloud acceptance gates remain unresolved. New green phone cases do not exonerate the old compositor fault. No native image or production release is claimed.

## Flat-roster correction (2026-09-15)

The user reported literal `\25be` text from a doubly escaped CSS arrow and a child disclosure containing only an ID. Removed the nested disclosure/marker and its roster-only styles; the expanded rail now lists description and phase without IDs. Running/queued/finishing inclusion and runtime protocol are unchanged.

Tests first: two regressions failed on the old nested markup/marker. A subsequent CSS assertion was corrected to target only the roster: historical N1 receipts still use the shared identity metadata style. **230 web unit tests**, typecheck and lint passed. **38 Chromium frontend cases passed**; the seven WebKit cases initially could not launch because the restarted host lacked `libgstreamer-1.0.so.0`. After `npx playwright install-deps webkit`, those **seven WebKit cases passed**, completing all 45 frontend cases without changing assertions/timeouts or repeating the completed Chromium cases. Existing broader release gates are unchanged.

[Separate checksum-indexed correction evidence](https://files---pi-orb-1077475695242.us-central1.run.app/s/58efed98-b832-4025-a899-7f43fed7ed72/qualification/pr-44/flat-roster-evidence.tar.gz): **37,686 bytes**, SHA-256 `5e14eb406b8fdd8edf5894a85f7cdf64eb8add4e4c7628e986769b9c7074a926`. All members were byte-verified; synthetic test/prerequisite logs only, including first failures. The 7400 frontend was explicitly restarted and its app/API responses verified; the existing 7200 controller and user work were not restarted.

## Preview inspection

A separate Vite frontend on **7400** proxies the existing **7200** process/PGlite service, using its own dependency cache. Neither existing controller was restarted; neither live static root was rebuilt. The user's existing orb was already stopped. Read-only browser inspection at desktop and 390px confirms three readable failed receipts, actual unsupported-model details, no raw task XML, no live rail and no horizontal overflow. Its history/credentials remain in the original service. New runtime code is loaded on its next user-initiated start; no work was started or steered for the inspection.
