Cancellation must remain effective during workspace preparation and session creation, and reach every resumed run. This rechecks cancellation before prompting, cleans up acquired resources before terminal reporting, and gives each resume a fresh abort controller.

Validation: 1,768 package tests passed; typecheck and lint passed.
