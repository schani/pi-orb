An embedded session's checkout may differ from the process working directory. This adds an explicit directory for custom-agent discovery, project settings, and workspace preparation, without changing process-wide state or the default behavior when omitted.

Validation: 1,767 package tests passed; typecheck and lint passed.
