diff --git a/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.d.ts b/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.d.ts
index 5620ac3..8006794 100644
--- a/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.d.ts
+++ b/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.d.ts
@@ -201,6 +201,7 @@ export declare class AgentSession {
     private _eventListeners;
     private _isAgentRunActive;
     private _agentRunAbortRequested;
+    private _agentRunPreparationAbortController;
     private _idleWaitPromise;
     private _resolveIdleWait;
     /** Tracks pending steering messages for UI display. Removed when delivered. */
diff --git a/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js b/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js
index 36fdb31..2afa34e 100644
--- a/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js
+++ b/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js
@@ -84,6 +84,7 @@ export class AgentSession {
     _eventListeners = [];
     _isAgentRunActive = false;
     _agentRunAbortRequested = false;
+    _agentRunPreparationAbortController;
     _idleWaitPromise;
     _resolveIdleWait;
     /** Tracks pending steering messages for UI display. Removed when delivered. */
@@ -821,6 +822,7 @@ export class AgentSession {
      */
     dispose() {
         try {
+            this._agentRunPreparationAbortController?.abort();
             this.abortRetry();
             this.abortCompaction();
             this.abortBranchSummary();
@@ -1075,11 +1077,93 @@ export class AgentSession {
     // =========================================================================
     // Prompting
     // =========================================================================
-    async _runAgentPrompt(messages) {
+    async _prepareAgentRun(prompt, images, signal) {
+        signal?.throwIfAborted();
+        this._flushPendingBashMessages();
+        this._flushPendingCustomMessages();
+        if (!this.model) {
+            throw new Error(formatNoModelSelectedMessage());
+        }
+        const hasConfiguredAuth = this._modelRuntime.hasConfiguredAuth(this.model.provider) ||
+            (await this._modelRuntime.checkAuth(this.model.provider, { signal })) !== undefined;
+        signal?.throwIfAborted();
+        if (!hasConfiguredAuth) {
+            const isOAuth = this._modelRuntime.isUsingOAuth(this.model.provider);
+            if (isOAuth) {
+                throw new Error(`Authentication failed for "${this.model.provider}". ` +
+                    `Credentials may have expired or network is unavailable. ` +
+                    `Run '/login ${this.model.provider}' to re-authenticate.`);
+            }
+            throw new Error(formatNoApiKeyFoundMessage(this.model.provider));
+        }
+        const lastAssistant = this._findLastAssistantMessage();
+        if (lastAssistant) {
+            await this._checkCompaction(lastAssistant, false);
+            signal?.throwIfAborted();
+        }
+        const selectedToolsBefore = this._baseSystemPromptOptions.selectedTools;
+        const result = await this._extensionRunner.emitBeforeAgentStart(prompt, images, this._baseSystemPromptOptions);
+        signal?.throwIfAborted();
+        const handlerEditedTools = result.systemPromptOptions.selectedTools.length !== selectedToolsBefore.length ||
+            result.systemPromptOptions.selectedTools.some((name, index) => name !== selectedToolsBefore[index]);
+        if (!handlerEditedTools)
+            result.systemPromptOptions.selectedTools = this.getActiveToolNames();
+        const messages = result.messages.map((msg) => ({
+            role: "custom",
+            customType: msg.customType,
+            content: msg.content ?? [],
+            display: msg.display,
+            details: msg.details,
+            timestamp: Date.now(),
+        }));
+        return { messages, systemPromptOptions: result.systemPromptOptions };
+    }
+    _applyAgentRunPreparation(prepared) {
+        const updateMessage = this._preparePromptAndToolLoadout(prepared.systemPromptOptions);
+        this._runSystemPromptOptions = prepared.systemPromptOptions;
+        return updateMessage;
+    }
+    async _runCustomMessagePrompt(appMessage) {
         this._agentRunAbortRequested = false;
         this._isAgentRunActive = true;
+        const preparationAbortController = new AbortController();
+        this._agentRunPreparationAbortController = preparationAbortController;
+        let preparationCompleted = false;
+        try {
+            const images = Array.isArray(appMessage.content)
+                ? appMessage.content.filter((part) => part.type === "image")
+                : undefined;
+            const prepared = await this._prepareAgentRun(contentText(appMessage.content, ""), images, preparationAbortController.signal);
+            const messages = [appMessage, ...prepared.messages];
+            const updateMessage = this._applyAgentRunPreparation(prepared);
+            if (updateMessage)
+                messages.unshift(updateMessage);
+            if (this._agentRunPreparationAbortController === preparationAbortController)
+                this._agentRunPreparationAbortController = undefined;
+            preparationCompleted = true;
+            await this._runAgentPrompt(messages, true);
+        }
+        catch (error) {
+            if (!preparationAbortController.signal.aborted)
+                throw error;
+        }
+        finally {
+            if (this._agentRunPreparationAbortController === preparationAbortController)
+                this._agentRunPreparationAbortController = undefined;
+            if (!preparationCompleted) {
+                this._isAgentRunActive = false;
+                this._resolveIdleWaitIfIdle();
+            }
+        }
+    }
+    async _runAgentPrompt(messages, runAlreadyActive = false) {
+        if (!runAlreadyActive) {
+            this._agentRunAbortRequested = false;
+            this._isAgentRunActive = true;
+        }
         try {
-            await this.agent.prompt(messages);
+            if (!this._agentRunAbortRequested)
+                await this.agent.prompt(messages);
             while (!this._agentRunAbortRequested) {
                 if (await this._handlePostAgentRun()) {
                     if (this._agentRunAbortRequested)
@@ -1253,41 +1337,9 @@ export class AgentSession {
                 preflightResult?.(true);
                 return;
             }
-            // Flush any pending bash and custom messages before the new prompt
-            this._flushPendingBashMessages();
-            this._flushPendingCustomMessages();
-            // Validate model
-            if (!this.model) {
-                throw new Error(formatNoModelSelectedMessage());
-            }
-            const hasConfiguredAuth = this._modelRuntime.hasConfiguredAuth(this.model.provider) ||
-                (await this._modelRuntime.checkAuth(this.model.provider)) !== undefined;
-            if (!hasConfiguredAuth) {
-                const isOAuth = this._modelRuntime.isUsingOAuth(this.model.provider);
-                if (isOAuth) {
-                    throw new Error(`Authentication failed for "${this.model.provider}". ` +
-                        `Credentials may have expired or network is unavailable. ` +
-                        `Run '/login ${this.model.provider}' to re-authenticate.`);
-                }
-                throw new Error(formatNoApiKeyFoundMessage(this.model.provider));
-            }
-            // Check if we need to compact before sending (catches aborted responses).
-            // The user's new prompt is sent below, so do not call agent.continue() here.
-            const lastAssistant = this._findLastAssistantMessage();
-            if (lastAssistant) {
-                await this._checkCompaction(lastAssistant, false);
-            }
-            // Emit before_agent_start before normalizing images so extension-driven model
+            // Prepare the agent run before normalizing images so extension-driven model
             // selection determines the resize profile used for the request and history.
-            const selectedToolsBefore = this._baseSystemPromptOptions.selectedTools;
-            const result = await this._extensionRunner.emitBeforeAgentStart(expandedText, currentImages, this._baseSystemPromptOptions);
-            // Handlers may edit event.systemPromptOptions.selectedTools or call setActiveTools(),
-            // which updates the live loadout instead. An explicit edit wins; otherwise the live
-            // loadout is authoritative, so a setActiveTools() call is not undone here.
-            const handlerEditedTools = result.systemPromptOptions.selectedTools.length !== selectedToolsBefore.length ||
-                result.systemPromptOptions.selectedTools.some((name, index) => name !== selectedToolsBefore[index]);
-            if (!handlerEditedTools)
-                result.systemPromptOptions.selectedTools = this.getActiveToolNames();
+            const prepared = await this._prepareAgentRun(expandedText, currentImages);
             const normalized = await this._normalizePromptImages(currentImages);
             const userText = normalized.hints.length > 0 ? `${expandedText}\n\n${normalized.hints.join("\n")}` : expandedText;
             // Build messages only after hooks and image normalization have completed.
@@ -1304,19 +1356,8 @@ export class AgentSession {
                 messages.push(msg);
             }
             this._pendingNextTurnMessages = [];
-            for (const msg of result.messages) {
-                messages.push({
-                    role: "custom",
-                    customType: msg.customType,
-                    // Untyped extensions can pass null/missing content; normalize at ingestion.
-                    content: msg.content ?? [],
-                    display: msg.display,
-                    details: msg.details,
-                    timestamp: Date.now(),
-                });
-            }
-            const updateMessage = this._preparePromptAndToolLoadout(result.systemPromptOptions);
-            this._runSystemPromptOptions = result.systemPromptOptions;
+            messages.push(...prepared.messages);
+            const updateMessage = this._applyAgentRunPreparation(prepared);
             if (updateMessage)
                 messages.unshift(updateMessage);
         }
@@ -1501,10 +1542,10 @@ export class AgentSession {
         }
         else if (options?.triggerTurn) {
             if (this._isEmittingAgentSettled) {
-                this._deferredSettledActions.push(async () => await this._runAgentPrompt(appMessage));
+                this._deferredSettledActions.push(async () => await this._runCustomMessagePrompt(appMessage));
                 return;
             }
-            await this._runAgentPrompt(appMessage);
+            await this._runCustomMessagePrompt(appMessage);
         }
         else if (this.isStreaming) {
             // Appending now would put the message between an assistant tool call and its
@@ -1609,6 +1650,7 @@ export class AgentSession {
         if (this._isAgentRunActive) {
             this._agentRunAbortRequested = true;
         }
+        this._agentRunPreparationAbortController?.abort();
         this.abortRetry();
         this.abortCompaction();
         this.abortBranchSummary();
@@ -2446,7 +2488,7 @@ export class AgentSession {
             getScopedModels: () => this._scopedModels,
             isIdle: () => this.isIdle,
             isProjectTrusted: () => this.settingsManager.isProjectTrusted(),
-            getSignal: () => this.agent.signal,
+            getSignal: () => this._agentRunPreparationAbortController?.signal ?? this.agent.signal,
             abort: () => {
                 if (this._extensionAbortHandler) {
                     this._extensionAbortHandler();
diff --git a/node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/runner.js b/node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/runner.js
index b8c238f..19befd8 100644
--- a/node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/runner.js
+++ b/node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/runner.js
@@ -1035,6 +1035,7 @@ export class ExtensionRunner {
                         systemPromptOptions: currentOptions,
                     };
                     const handlerResult = await handler(event, ctx);
+                    ctx.signal?.throwIfAborted();
                     if (handlerResult) {
                         const result = handlerResult;
                         if (result.message)
@@ -1045,6 +1046,7 @@ export class ExtensionRunner {
                     }
                 }
                 catch (err) {
+                    ctx.signal?.throwIfAborted();
                     const message = err instanceof Error ? err.message : String(err);
                     const stack = err instanceof Error ? err.stack : undefined;
                     this.emitError({
diff --git a/node_modules/@earendil-works/pi-coding-agent/dist/core/model-runtime.js b/node_modules/@earendil-works/pi-coding-agent/dist/core/model-runtime.js
index 81bf752..6d84f8f 100644
--- a/node_modules/@earendil-works/pi-coding-agent/dist/core/model-runtime.js
+++ b/node_modules/@earendil-works/pi-coding-agent/dist/core/model-runtime.js
@@ -438,12 +438,21 @@ export class ModelRuntime {
         const env = resolution.env || providerOptions.env
             ? { ...(resolution.env ?? {}), ...(providerOptions.env ?? {}) }
             : undefined;
+        const apiKey = providerOptions.apiKey ?? resolution.auth.apiKey;
+        let requestDiagnostics;
+        try {
+            requestDiagnostics = this.extensionProviders.get(model.provider)?.getRequestDiagnostics?.(apiKey);
+        }
+        catch {
+            // Diagnostic metadata must never block the provider request.
+        }
         return {
             provider,
             model: resolution.auth.baseUrl ? { ...model, baseUrl: resolution.auth.baseUrl } : model,
             options: {
                 ...providerOptions,
-                apiKey: providerOptions.apiKey ?? resolution.auth.apiKey,
+                apiKey,
+                ...(requestDiagnostics ? { codexBrokerDiagnostics: requestDiagnostics } : {}),
                 headers,
                 env,
             },
diff --git a/node_modules/@earendil-works/pi-coding-agent/dist/core/provider-composer.d.ts b/node_modules/@earendil-works/pi-coding-agent/dist/core/provider-composer.d.ts
index a7b3301..8eb17fc 100644
--- a/node_modules/@earendil-works/pi-coding-agent/dist/core/provider-composer.d.ts
+++ b/node_modules/@earendil-works/pi-coding-agent/dist/core/provider-composer.d.ts
@@ -21,6 +21,7 @@ export interface ProviderConfigInput {
     streamSimple?: (model: Model<Api>, context: TranscriptContext, options?: SimpleStreamOptions) => AssistantMessageEventStream;
     headers?: Record<string, string>;
     authHeader?: boolean;
+    getRequestDiagnostics?(apiKey: string): { brokerGeneration?: number; tokenExpiresAt: number } | undefined;
     oauth?: ExtensionOAuthConfig;
     models?: Array<{
         id: string;
