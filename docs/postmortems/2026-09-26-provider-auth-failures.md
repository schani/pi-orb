# Provider authentication failures after an orb wake

## Observed sequence (UTC)

Orb `b7e8855f-5315-4d5a-a81f-fa275e100b98` scheduled sleep on 2026-09-25 at 22:14:02, woke at 23:14, and was runtime-ready at 23:15:15. At 23:15:15.954, its `openai-codex` / `openai-codex-responses` / `gpt-6-astra` response stream closed with WebSocket code 1011, phase `after_message_stream_start`. A retry at 23:15:18.465 returned `Incorrect API key sk-[redacted]`. This provider uses ChatGPT OAuth, not a user API key; the origin of that key-shaped value is unknown. The orb idle-stopped at 23:30. Inference worked after a restart at 02:55 on September 26. Neither the failure's cause nor a causal role for the restart is established.

For the Datadog MCP connection `e2c2ee5c…`, read-only production SQL inspection of `mcp_oauth_events` shows a successful refresh to generation 89 at 2026-09-25 22:13:12.104770, invalidation of generation 90 at 2026-09-26 02:56:05.549676, and `credential_destroyed` for generation 89 at 02:59:05.174386. The current credential pointer is null. Generation 89 proves live production refresh worked; these events do not identify why generation 90 was invalidated. The adapter maps upstream `invalid_grant` and `invalid_client`, an absent refresh token, and a successful response with an unusable token shape to the same `invalid_grant` outcome. Prior successful refresh and refresh-token preservation in code argue against a missing refresh token, but cannot establish the exact subcause. The retained pi-orb diagnostics do not distinguish these causes.

## Boundary and recovery

The Datadog connection requires browser reauthorization; an already-configured running orb can use the new grant on its next explicit operation without restarting. No evidence links the Datadog invalidation to the Codex failure, or proves that restarting caused either failure or recovery. Keep these as separate incidents of unknown cause. Safe, typed refresh-failure diagnostics are implemented locally, not deployed (`docs/mcp.md`); remote bodies and secrets remain excluded. The historical exact cause remains unknown. Controlled Datadog revocation/refresh qualification remains incomplete (`docs/mcp.md`). Codex failure-only diagnostics are implemented locally but not deployed (`docs/credentials.md`); they identify attempt context, not the cause of this historical failure.

## Diagnostic validation boundary (2026-09-26)

A code agent launched its final focused provider test **during** the parent's clean `npm ci`. `/tmp/final-provider` records `npm warn exec The following package was not found and will be installed: vitest@5.0.2`, then six diagnostic-absence failures in `provider.contract.test.ts`; the stack read the unpatched nested coding-agent `pi-ai` stream. The dependency tree was being mutated while the test ran, so this is an invalid test boundary, not evidence of an intermittent product failure. The first failure is retained. The boundary correction is to finish installation, freeze dependency writers, and only then run tests; clean `npm ci` subsequently applied all three patches. The subsequent process E2E passed, but full `npm test` exited 1 on an unhandled worker RPC timeout without failed assertions (`docs/postmortems/2026-09-26-unit-worker-rpc-timeout.md`). A passing rerun alone would not explain the failure; the concurrent install and unpatched module path do.
