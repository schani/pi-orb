# Open questions

Genuinely undecided design questions. Numbering is frozen and append-only — resolved questions are marked in place, never renumbered or deleted, because code and docs reference questions by number. Actionable work items (bugs, hardening, agreed follow-ups) live in `TODO.md`, not here; when a question is decided and the decision implies work, it is marked resolved here and the work moves there in the same edit.

## Immediate architecture

1. Finalize the remaining HTTP/WebSocket payload details, capability negotiation, and versioning rules.
2. Fully resolved by the Cloud Run WebSocket validation exercise (2026-07-30; its throwaway runbook and code lived under `experiments/cloudrun-ws-validation/` and were removed after the exercise — see git history): the request timeout must be raised from the 300-second default (measured forced close at 301 s) to the 60-minute maximum (measured forced close at 3601 s, code 1006, clean ~1 s reconnect — the ordinary resynchronization path handles it); Direct VPC egress carries a WebSocket from Cloud Run to an internal-IP COS VM with no external addresses involved, and such outbound connections are exempt from the request timeout (measured 11.4 h, zero drops, ended only by instance replacement); direct IAP-on-Cloud-Run is available, gates unauthenticated traffic, and passes browser WebSockets after sign-in; billable instance time is a flat one instance regardless of open sockets under instance-based billing (measured over 12+ hours). The load-balancer fallback is unnecessary.

## Replication and history

3. Define stable adapter-generated IDs for harnesses that do not provide native record IDs.
4. Compare real Claude Code and Codex persisted history examples before freezing the normalized schema.
5. Decide which additional record variants deserve static types beyond message, compaction, and generic event.
6. Decide how to replicate and store images, large outputs, truncated outputs, patches, and artifacts.
7. Define what the UI indicates, if anything, when live history is newer than the replica.

## Lifecycle and background work

8. Resolved: Pi SDK 0.83.0 has no shell registry or reliable query for surviving agent-started processes; only active awaited execution/tool lifecycle is observable (docs/lifecycle.md).
9. Determine whether ordinary OS process/cgroup inspection is reliable enough to avoid a custom background-job tool.
10. Resolved: a browser connection prevents automatic idle shutdown only while its tab reports `visible`; hidden or non-reporting connections do not count (docs/lifecycle.md).

## Project and environment

11. Partially resolved and implemented 2026-08-09: any returned runtime-health `failed`, including `clone_failed`, is terminal for that host boot and promptly becomes durable/user-visible orb `failed`; retry work exists only while runtime health remains `initializing` (`docs/lifecycle.md`, `docs/postmortems/2026-08-09-private-repo-clone-outside-github-app-installation.md`). Still define repository-access preflight, whether/how the runtime performs bounded clone retries before returning failure, default-branch behavior, and recorded repository metadata.
12. Choose the runtime container base-image pin and Node 24 release/update policy. (The VM host-OS half of this question dissolved: cloud hosts boot Container-Optimized OS and only run the runtime container, docs/host-provider.md.)
13. Resolved (2026-08-25): adopt both, as `.agents/setup` and `.agents/resume` with Amp's split — `docs/orb-setup-hook.md`; the general form is question 41.
14. Decide how setup caching/prebuilt snapshots work after the unoptimized first slice.
15. Decide which tools and services are installed in the prescribed base image. (Resolved for `gh`: installed in the runtime image with brokered auth, `docs/credentials.md`. Resolved for Python on 2026-08-08: Python 3, the `python` alias, and virtual-environment support are installed by default; global pip is not. Resolved for Rust on 2026-08-09: pinned rustup and native build prerequisites are image-resident, while a default stable toolchain and Cargo state live in each orb's persistent home, `docs/host-provider.md`. Resolved for ZIP archives on 2026-08-12: `zip` and `unzip` are image-resident, `docs/host-provider.md`. The broader tool list remains open.)
16. Decide if/when an Orbfile is introduced and what it is allowed to configure.
17. Decide how services, ports, logs, browser automation, and preview URLs work. (Ports/preview URLs resolved 2026-08-05: tier-1 Tailscale port exposure, docs/ports.md — every orb joins the user's tailnet, control-plane-generated `http://pi-orb-<orbId>.<tailnet>.ts.net:<port>` URLs. Browser automation resolved 2026-08-09: pinned `agent-browser` plus system Chromium are installed by default and advertised in the system prompt, `docs/host-provider.md`. Logs remain open.)

## Control plane, database, and deployment

18. Define the abstract history repository/database interface.
19. Resolved: OpenTofu manages the static infrastructure plane; per-orb VMs stay dynamic provider resources outside IaC (docs/deployment.md).
20. Decide how to partition polling later if redundant all-orb polling becomes inefficient at scale; no leader or partitioning is needed initially.
21. Resolved: credential-broker and per-orb-token design written (docs/credentials.md); networking decided: Direct VPC egress to internal instance IPs (docs/host-provider.md). Remaining: implement it, and validate direct IAP-on-Cloud-Run during the WebSocket validation exercise.
22. Define observability, audit logging, metrics, and cost attribution.
23. Resolved: the orphan-host sweep in docs/lifecycle.md is the decided design — a periodic idempotent loop over `listManagedHosts` that only ever stops pi-orb-labeled hosts, never starts or deletes.

## Product and security

24. Define the future user/project/orb identity and authorization model before public deployment.
25. Define future per-user/project model credentials and private-Git credentials/workload identity. (A proposal for brokered GitHub credentials exists in docs/credentials.md.)
26. Define project trust and the security boundary for repository-controlled code.
27. Partially resolved 2026-08-08: orb deletion removes the authoritative filesystem and replicated history rather than retaining a browsable transcript; the implemented resource inventory and cleanup protocol are in `docs/orb-deletion.md`. Export-before-delete remains undecided.
28. Define whether stopped hosts have an expiration/garbage-collection policy.
29. Define the eventual suborb orchestration and filesystem handoff model.

## Testing

30. Define CI iteration budgets and storage/replay conventions for failing entropy traces.
## Cloud slice follow-ups

31. Moved to `TODO.md` (agreed follow-up, not an open question): a standalone re-login action for mid-run credential revocation.
32. Resolved (2026-08-01): the script-version stamp is implemented in `GceOrbHostProvider` — see the docs/host-provider.md rollout-caveat entry for the mechanism, the upgrade-delivery side effect, and the accepted drain-window residual. Superseded 2026-08-12: `docs/compute-replacement.md` removes the script stamp and in-place repair entirely; a host whose committed specification is stale is replaced on its next Start, and the deploy generation survives only as the forward-only replacement fence.
33. Moved to `TODO.md` (hardening work, not an open question): closing the served-vs-durable persistence gap (incident: `docs/postmortems/2026-08-03-cursor-not-found.md`).
34. Moved to `TODO.md` (bug, not an open question): the full-slice E2E cannot run on macOS Docker Desktop.
35. Decide whether to build the exe.dev host provider evaluated in docs/host-provider.md (2026-08-05). Prerequisites before deciding: empirically verify WebSocket forwarding through the exe.dev HTTPS proxy, `ls --json` observability fields, duplicate-name create behavior, and in-VM halt semantics; choose an emulation strategy for `stop` (or accept always-on with idle-stop disabled for this provider); choose an image-upgrade story for existing VMs (the rootfs is the persistent disk, so re-imaging destroys `/workspace`); and accept flipping the runtime broker Cloud Run service to public ingress.
36. Moved to `TODO.md` (bug, not an open question): the unreachable-restart livelock (forensics: `docs/postmortems/2026-08-05-unreachable-restart-livelock.md`).
37. Decide whether to build the AWS Lambda MicroVMs host provider evaluated in docs/host-provider.md (full writeup: docs/AWS-MICROVMS.md) (2026-08-05). The 8 h `maximumDurationInSeconds` cap (running + suspended combined; terminated VMs are unrecoverable) is survivable only by externalizing durable orb state — per-orb EFS over a VPC egress connector is the proposed mechanism — plus proactive VM rotation in the lifecycle machine. Prerequisites before deciding: empirically verify EFS/NFS mounting from inside a MicroVM through a VPC egress connector, NFS mount survival across suspend/resume, git performance on EFS, the `/terminate` hook timeout, auth-token maximum expiry, and per-VM tagging/enumeration; decide the rotation design (drain window, age tracking, successor handoff); accept the cross-cloud operational surface (AWS account, IAM, VPC, EFS alongside GCP); accept ARM64-only runtime images and public-egress runtime broker with JWE token refresh.
38. Decide whether to surface control-plane lifecycle events (host restarts, unreachable-restarts, idle stops, transitions with reasons) to the user in the orb transcript (2026-08-05; motivated by `docs/postmortems/2026-08-05-egress-blip-false-unreachable-restart.md`, where a restart killed a turn with no user-visible trace — the orb just "stopped turning"). Current proposal: persist the reconciler's existing edge events (the 2026-08-06 event-log vocabulary and noise rule, docs/lifecycle.md) into a per-orb `orb_events` table written in the same transaction as the state-transition CAS (exactly-once, ordered, tiny volume since a healthy orb emits nothing); expose them read-only (an `events` array on `OrbHistoryView`, or a small events endpoint); the web UI interleaves them by timestamp into the transcript as visually distinct system notices ("⟳ host restarted: runtime unreachable for 51s"), delivered via the existing orb/history polling. This generalizes the already-decided user-visible lifecycle facts (`stop_reason`, `stateDetail` in docs/control-plane-api.md) from latest-state to a timeline. Rejected: synthesizing records into the replicated history — the Postgres history must remain a verbatim prefix of the runtime session file (cursor CAS, pull-response integrity validation, head-ID chaining and the browser's `stale_head` gate all assume it), and a runtime-appended marker would put UI copy into the model's context and cannot be written by a runtime that is down, which is exactly when the line is needed; also rejected: injecting synthesized frames into the live WebSocket, which would mix the control plane into a deliberately dumb browser↔runtime pipe. Distinct from the interrupted-turn resume follow-up in `TODO.md`, whose marker should be a session record precisely because the model must see it: control-plane event = display-only infrastructure truth, session record = agent context; they coexist. Related: question 7 (UI indication when live history is newer than the replica).
39. Resolved (2026-08-09): implemented the provider-neutral interactive terminal in `docs/terminal.md` — wterm in the orb page → dedicated binary-capable control-plane WebSocket proxy → an ephemeral `node-pty` Bash PTY owned by the standard runtime. It adds no `OrbHostProvider`, database, history, lifecycle, or Tailscale contract. Sessions are one-WebSocket/one-shell with no tmux, detach, shared screen, persistence, or reconnect replay; they remain distinct from persisted composer shell commands. Official `node-pty` is source-built in the prescribed image, macOS uses its bundled prebuilds, and runtime admission/cleanup has unit, deterministic-schedule, proxy, and full-slice E2E coverage.
40. Decide the continuous-deployment release policy before implementing the GitHub Actions proposal in `docs/deployment.md` (researched 2026-08-09): whether “every push” means default latest-eligible coalescing, GitHub's bounded `queue: max` despite its non-chronological ordering guarantee, or a literal durable FIFO; whether the production environment is automatic or requires approval; whether hosted-runner smoke may omit the Tailscale preview leg or needs a tailnet-connected probe; and where rollback stops being automatic when a release includes database migrations or static-plane changes. The migration branch of this decision must define change detection, approval or maintenance policy, a verified recovery point, and a rehearsed forward-fix/rollback runbook. In every variant, the running apply/IAP-repair/smoke sequence is non-cancellable and serialized, a durable allocator shared with manual releases reserves a strictly increasing deploy generation (which, once `docs/compute-replacement.md` lands, fences host-spec replacement rather than script repair), IAP repair runs unconditionally after an attempted apply, GitHub-to-GCP authentication uses narrowly admitted Workload Identity Federation rather than a service-account key, and rollback advances the generation rather than restoring an old revision.

## Orb boot hooks

41. Define a per-project boot setup hook — the analogue of Amp's `.agents/setup` (2026-08-25; the general form of question 13, which asked only whether to adopt one). Decide **what** runs (a single well-known repository script, an Orbfile stanza, or a list of commands), **when** it runs (first boot only, every start, after compute replacement, after a checkout update), what it may assume (network, brokered credentials, a durable `$HOME` that may already carry the previous run's state), and how failure is reported (a `lifecycle:` event and a user-visible orb state, not silent stdout). The trust implication is smaller than it looks: repository code already runs as the agent in an orb, so a boot hook grants no new authority — it only moves the moment of execution earlier than the human's first prompt, which is precisely what makes an unattended orb useful and what makes a malicious commit take effect without anyone reading it. Motivating case: `apps/orb-runtime/skills/cloud-identity/SKILL.md` ends its Google Cloud flow by committing a non-secret external-account configuration plus a setup script, because the *trust* side of workload identity is per GCP project and permanent while the *in-orb* side — write the credential file, export three variables, run the `--cred-file` login — has to be redone in every new orb. With a boot hook that is zero steps; without one the next orb's agent or human has to run the script deliberately, and the skill must say so rather than imply it happens by itself. See `docs/workload-identity.md`. **Resolved (2026-08-25):** requirements in `docs/orb-setup-hook.md` — setup runs on the first boot of every compute incarnation with identity removed from its environment, resume runs on every start with identity, failure is user-visible and the orb still starts; implementation tracked in `TODO.md`.
42. Decide whether the orb runtime should run as an unprivileged user with passwordless `sudo`, as Amp does (2026-08-25, from `docs/orb-setup-hook.md`). Today the runtime is root and the image has no `sudo`; the hooks decision installs `sudo` so Amp-style scripts work unchanged and defers the user change, because it touches the persistent home's ownership, `node-pty`, Tailscale, and every provider's launch path. Decide what the user boundary would actually protect in an orb where repository code already runs as the agent, and whether it is worth the migration.
43. Decide whether pi-orb needs a repository-declared services manifest and supervisor — the analogue of Amp's `.amp/services.yaml` — for long-running processes an orb should keep alive across agent turns and restarts (2026-08-25, from `docs/orb-setup-hook.md`, which forbids long-running processes in hooks the way Amp does). Interacts with port exposure (`docs/ports.md`) and with the runtime's own supervision of the agent session.
44. Resolved (2026-08-28): the first configurable environment surface is project-scoped secrets only, delivered as one authenticated runtime snapshot on every process boot after identity-free setup and before resume. The complete secret set is one immutable secret-store bundle behind an exact-version PostgreSQL pointer; values never enter PostgreSQL, browser reads, provider metadata, or persistent orb storage. No plain variables, overrides, secret files, setup exceptions, hot update, applied-revision tracking, or audit table initially. Full contract and rationale: `docs/credentials.md`.
45. Resolved (2026-08-28): use the Sealed card modal for project secrets. It keeps the project dashboard recognizable, focuses the write-only interaction, and avoids introducing permanent panel height or a general project-settings architecture. The selected direction and four rejected alternatives remain in `design-prototypes/project-secrets.html`; rationale and interaction requirements are in `docs/web-ui.md`.
