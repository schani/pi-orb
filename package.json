{
  "name": "pi-orb",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/protocol",
    "apps/control-plane",
    "apps/orb-runtime",
    "apps/web"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "typescript": "7.0.2",
    "vitest": "^3.0.0"
  }
}
